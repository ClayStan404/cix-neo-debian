#!/usr/bin/make -f

%:
	dh $@

override_dh_auto_configure override_dh_auto_build override_dh_auto_test:

override_dh_fixperms:
	dh_fixperms
	find debian/cix-audio-sof/usr/lib/firmware -type f -exec chmod 0644 -- {} +
